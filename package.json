{
  "name": "regex-parity",
  "version": "0.0.0",
  "private": true,
  "description": "The same regex gives different answers in JavaScript, Python and Java on real-world text. regex-parity makes regex rules agree across languages, and proves it.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "evidence": "bash test/evidence/run.sh"
  },
  "devDependencies": {
    "re2js": "2.8.6",
    "yaml": "2.9.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YauhenBichel/regex-parity.git"
  }
}
